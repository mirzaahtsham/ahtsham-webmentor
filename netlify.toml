[build]
  command = "npm ci && npm run build"
  publish = ".next"
  
[build.environment]
  NODE_VERSION = "20"          # explicitly force Node 20
  TAILWIND_DISABLE_OXIDE = "1"

[[plugins]]
package = "@netlify/plugin-nextjs"
